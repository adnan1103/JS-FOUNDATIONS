"use strict"; // treat all JS code as newer version 

// alert (3 + 3 ) // we are using node js, node browser

console.log(3 + 
    3);  // code readability should be higher


    let name = "Adnan";
    let age = 20
    let isLoggedIn = false
    let state;

    // Premptive data types

    // number => 2 to power of 53 (Maximum memory range)
    // bigint  use for large values
    // string => ""
    // boolean => true/ false
    // null => stande alone value (representation of empty value)
    // undefined => value yet not define
    // symbol => unique


    // NonPremptive data types
    // object 
    console.log(typeof age);
    console.log(typeof undefined);  // undefined
    console.log(typeof null);


